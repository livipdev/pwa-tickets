export const NAME = 'user';
export const TEST_API = '/api/test';

const TEST_UPDATE = 'USER::TEST::UPDATE';
const TEST_START = 'USER::TEST::START';
const TEST_FINISH = 'USER::TEST::FINISH';
const TEST = {
  UPDATE: TEST_UPDATE,
  START: TEST_START,
  FINISH: TEST_FINISH,
};

export const ACTION_TYPES = {
  TEST,
};
